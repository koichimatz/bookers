<% if flash[:notice] %>
  <div class="alert alert-primary" role="alert">
    <%= notice %>
  </div>
<% end %>

<h1>Editing Book</h1>
<!--<form class="edit_book" id="edit_book.id" action="/books/(book.id)" acccept-charset="UTF-8" method="post">-->
<% if @book.errors.any? %>
 <%= @book.errors.count%> errors prohibited this book from being saved:
 <% @book.errors.full_messages.each do |message|%>
  <%= message %>
 <% end %>
<% end %>

<%= form_with model:@book, url:"/books/#{@book.id}", local:true do |f| %>
<h4>Title</h4>
<%= f.text_field:title %>
<h4>Body</h4>
<%= f.text_area:body %>
<p>
<%= f.submit'Update Book' %>
</p>
<% end %>

<%= link_to"Show", book_path(@book.id) %> |
<%= link_to"Back", books_path %>

<!--https://web-camp.online/lesson/curriculums/212/contents/1438-->